workflow:
  steps:
    - set_flags:
        flags:
          - type: build
            status: enable
            project: home:Admin
            architecture: x86_64
            repository: openSUSE_Tumbleweed
  filters:
    event: pull_request
